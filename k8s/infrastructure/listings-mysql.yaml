apiVersion: v1
kind: PersistentVolume
metadata:
  name: listings-mysql-pv
  namespace: auction-infrastructure
  labels:
    type: local
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data/listings-mysql"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: listings-mysql-pvc
  namespace: auction-infrastructure
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: listings-mysql
  namespace: auction-infrastructure
spec:
  replicas: 1
  selector:
    matchLabels:
      app: listings-mysql
  template:
    metadata:
      labels:
        app: listings-mysql
    spec:
      containers:
      - name: listings-mysql
        image: mysql:8.0
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secrets
              key: LISTINGS_MYSQL_ROOT_PASSWORD
        - name: MYSQL_DATABASE
          value: listings
        volumeMounts:
        - name: listings-mysql-persistent-storage
          mountPath: /var/lib/mysql
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
      volumes:
      - name: listings-mysql-persistent-storage
        persistentVolumeClaim:
          claimName: listings-mysql-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: listings-mysql
  namespace: auction-infrastructure
spec:
  selector:
    app: listings-mysql
  ports:
  - port: 3306
    targetPort: 3306
  type: ClusterIP
