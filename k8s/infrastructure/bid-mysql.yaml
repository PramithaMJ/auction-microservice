apiVersion: v1
kind: PersistentVolume
metadata:
  name: bid-mysql-pv
  namespace: auction-infrastructure
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data/bid-mysql"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bid-mysql-pvc
  namespace: auction-infrastructure
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bid-mysql
  namespace: auction-infrastructure
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bid-mysql
  template:
    metadata:
      labels:
        app: bid-mysql
    spec:
      containers:
      - name: bid-mysql
        image: mysql:8.0
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secrets
              key: BID_MYSQL_ROOT_PASSWORD
        - name: MYSQL_DATABASE
          value: bids
        volumeMounts:
        - name: bid-mysql-persistent-storage
          mountPath: /var/lib/mysql
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
      volumes:
      - name: bid-mysql-persistent-storage
        persistentVolumeClaim:
          claimName: bid-mysql-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: bid-mysql
  namespace: auction-infrastructure
spec:
  selector:
    app: bid-mysql
  ports:
  - port: 3306
    targetPort: 3306
  type: ClusterIP
