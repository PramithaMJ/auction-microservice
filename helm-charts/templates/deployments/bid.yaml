apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.bid.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.services.bid.name }}
    {{- include "auction.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.services.bid.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.services.bid.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.bid.name }}
    spec:
      containers:
        - name: {{ .Values.services.bid.name }}
          image: "{{ .Values.global.imageRegistry }}/{{ .Values.services.bid.image.repository }}:{{ .Values.services.bid.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: 3000
          env:
            - name: MYSQL_HOST
              value: {{ .Values.services.bid.database.host }}
            - name: MYSQL_DATABASE
              value: {{ .Values.services.bid.database.name }}
            - name: MYSQL_PORT
              value: "{{ .Values.services.bid.database.port }}"
          envFrom:
            - configMapRef:
                name: auction-config
