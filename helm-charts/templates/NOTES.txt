Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

The application has been deployed to namespace: {{ .Values.namespace }}

{{- if .Values.ingress.enabled }}
You can access the application at:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else }}
Get the application URL by running these commands:
  export POD_NAME=$(kubectl get pods --namespace {{ .Values.namespace }} -l "app={{ .Values.services.apiGateway.name }}" -o jsonpath="{.items[0].metadata.name}")
  export CONTAINER_PORT=$(kubectl get pod --namespace {{ .Values.namespace }} $POD_NAME -o jsonpath="{.spec.containers[0].ports[0].containerPort}")
  kubectl --namespace {{ .Values.namespace }} port-forward $POD_NAME 8080:$CONTAINER_PORT
  echo "Visit http://127.0.0.1:8080 to use your application"
{{- end }}

{{- if .Values.infrastructure.jaeger.enabled }}
To access Jaeger UI:
  kubectl port-forward svc/jaeger-query 16686:16686 -n {{ .Values.namespace }}
  Visit http://localhost:16686
{{- end }}
